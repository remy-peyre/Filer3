{% extends 'userAccount.html.twig' %}

{% block content %}
    <section class="folder">
        {% for error in errors %}
            {{ error }}<br>
        {% endfor %}

        <form class="form_create_folder" method="POST" action="?action=folder">
            <p class="center_option">New folder :</p>
            <p>Create a folder<br>
                <input type="text" name="folder_name" placeholder="Name folder">
                <select name="wich_folder">
                    <option value="0">Dossier principal</option>
                    {% for folder in allFolders %}
                        <option value="{{folder.id}}">{{folder.foldername}}</option>
                    {% endfor %}
                </select>
                <input type="submit" value="Create folder">
            </p>
        </form>


        <form class="form_create_folder" method="POST" action="?action=folder">

            <p class="center_option">Move a folder in a folder:</p>
            <p>Select a folder, where you want him to go<br>
                <select name="wich_folder">
                    <option value="0">Dossier principal</option>
                    {% for folder in allFolders %}
                        <option value="{{folder.id}}">{{folder.foldername}}</option>
                    {% endfor %}
                </select>
                <select name="wich_folder">
                    <option value="0">Dossier principal</option>
                    {% for folder in allFolders %}
                        <option value="{{folder.id}}">{{folder.foldername}}</option>
                    {% endfor %}
                </select>
                <input type="submit" value="Move folder">
            </p>
        </form>

        <div class="row_show_folder_in_folder">
            {% for folder in allFolders %}
                <div class="colum_show_folder_in_folder">

                    <form class="form_folder_delete" method="POST" action="?action=yourFiles">
                        <input class="none" type="text" name="id_file_to_delete" value="{{file.id}}">
                        <input type="submit" value="">
                    </form>

                    <img class="size_folder_upload" src="web/image/folder.png" alt="{{file.filepath}}">

                    <div class="row_option_one_picture">
                        <!--<a href="{{file.filepath}}" download="{{file.filepath}}">
                            <img class="logo_download" src="web/image/download.png" alt="download"/>
                        </a>-->
                        <div class="rename_folder_button">Rename</div>
                    </div>
                    <form class="form_rename_folder none" method="POST" action="?action=yourFiles">
                        <input class="none" type="text" name="id_file_to_rename" value="{{file.id}}">
                        <input type="text" name="new_name" value="" placeholder="NewName -> NewName.ext">
                        <input type="submit" value="Rename">
                    </form>

                    {{ folder.foldername }}

                    <!--<form class="go_in_folder" method="POST" action="?action=switchCurrentFolder">
                        <input type="hidden" name="new_current_folder" value="{{folder.id}}">
                        <input type="submit" value="go to {{folder.foldername}}">
                    </form>-->

                </div>
            {% endfor %}
        </div>
    </section>
{% endblock %}


