<!DOCTYPE html>
<html>
<head>
    <title>FILER 3</title>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0 minimum-scale=0.2">
    <!--<link rel="icon" type="image/png" href="">-->
    <link rel="stylesheet" href="web/style/style.css">
    <link href="https://fonts.googleapis.com/css?family=Open+Sans" rel="stylesheet">
</head>
<body>
    <header class="header_user_account">

        <div class="width_title_menu">UPTOFILES</div>
        <div class="width_first_menu">
            <div class="width_first_option">
                <img src="">
                <div>Your Files</div>
            </div>
            <div class="width_seconde_option">Profil</div>
        </div>

        <div class="width_logout">
            <a href="?action=logout">
                Logout
            </a>
        </div>
    </header>
    <main>
        <section class="user_account">
            <div class="column_left">
                <p>Welcome, {{ user.username }}</p>
            </div>


            <div class="area_right">
                <div class="first_row_option">
                    <div class="button_option">
                        <a href="?action=upload_file">
                            Upload
                        </a>
                    </div>
                    <div class="button_option">
                        <a href="?action=upload_file">
                            Upload
                        </a>
                    </div>
                    <div class="button_option">
                        <a href="?action=upload_file">
                            Upload
                        </a>
                    </div>
                </div>
                <div class="column_right_containte">
                    <div>Your Uploads</div>
                    <div>No files found. Click to
                        <a href="?action=upload_file">
                            Upload
                        </a>
                    </div>
                    {% if error is defined and error is not empty %}
                        <p class="error">Error : {{ error }}</p>
                    {% endif %}


                    {% block content %}{% endblock %}

                    <!--
                    <div>
                        {% for error in errors %}
                            {{ error }}<br>
                        {% endfor %}
                    </div>
                    <div>
                        {% for file in allFiles %}
                            <p> {{file.type}}</p>
                            {% if file.type == "image" %}
                                <img src="{{file.filepath}}" alt="{{file.filename}}"></img><br><p>{{file.filepath}}</p>
                            {% endif %}
                        {% endfor %}
                    </div>
                    <form method="POST" action="?action=upload_file" enctype="multipart/form-data">
                        <p>UPLOAD :</p>
                        <p>Pick a File<br>
                            Click on Open and after Upload<br>
                            Files .jpg / .jpeg / .png / .pdf / .txt / .mp3 / .mp4
                            <br>
                            <input type="file" name="uploaded_file">
                            <input type="txt" name="initial_new_name">
                            <input type="submit" value="Upload">
                        </p>
                    </form>-->
                </div>

            </div>

        </section>


    </main>
</body>
</html>
